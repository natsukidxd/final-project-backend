# Database (using Supabase pooler for stable connections)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
DATABASE_URL=

# JWT
JWT_SECRET=

# CORS
CORS_ORIGIN=http://localhost:4200

# Cookies
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# SMTP (Ethereal - for development)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Server
PORT=4000
NODE_ENV=development